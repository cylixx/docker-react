language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t cylixx/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true cylixx/docker-react npm run test